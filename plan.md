# Explorer for Mac 開発計画

## 1. 基本方針

- macOSネイティブアプリとして開発する
- Swift＋AppKitを中心とし、必要な箇所だけSwiftUIを使用する
- 見た目はmacOS、情報設計と操作感はWindows Explorerを目指す
- ローカルファイルをMVPの対象とする
- Finderは削除・改変せず共存させる
- Windows風とMac風のキーマップを切り替え可能にする
- ファイル操作の安全性をUIより優先する

## 2. MVPの完成条件

次の操作だけで、日常的なローカルファイル管理ができる状態をMVPとする。

- フォルダーツリーから移動できる
- 詳細一覧で名前、日時、種類、サイズを確認できる
- 戻る、進む、上へ移動できる
- パスを直接入力できる
- タブを複数開ける
- コピー、切り取り、貼り付け、移動、名前変更、削除ができる
- ファイル競合を安全に処理できる
- コピーや移動の進捗を確認・キャンセルできる
- キーボード中心でも操作できる
- アプリ再起動後にタブ、表示設定、履歴を復元できる

## 3. フェーズ別実装計画

### Phase 0：仕様の固定

ExplorerとFinderの挙動を比較し、再現対象を文章化する。

作成する仕様：

- 画面ワイヤーフレーム
- Explorer挙動比較表
- キーボードショートカット一覧
- ファイル競合時のルール
- エラー表示方針
- MVPと将来機能の境界

先に決める項目：

- Windows風キー操作をどこまで再現するか
- ダブルクリック／シングルクリックの挙動
- フォルダーごとの表示設定を保存するか
- macOSアプリの「パッケージ」をファイルとフォルダーのどちらとして扱うか
- シンボリックリンクの扱い
- Mac App Store配布を目指すか、直接配布にするか

### Phase 1：プロジェクト基盤

Xcodeプロジェクトと基本アーキテクチャを作る。

主なモジュール：

```text
AppShell
├── Navigation
├── FolderTree
├── FileList
├── Preview
├── FileOperations
├── Search
├── MacIntegration
└── Persistence
```

初期データモデル：

- `FileItem`
- `DirectorySnapshot`
- `NavigationState`
- `TabState`
- `ViewSettings`
- `FileOperation`
- `OperationConflict`

この段階でユニットテストとUIテストのターゲットも用意する。

### Phase 2：読み取り専用ファイルブラウザー

まず、ファイルを変更しない安全な閲覧機能を完成させる。

実装対象：

- フォルダーツリー
- ファイル詳細一覧
- パスバー
- 戻る／進む／上へ
- タブ
- 並べ替え
- 隠しファイル表示
- ファイル情報取得
- フォルダー変更の監視
- 外付けボリュームの検出

完了条件：

- 1万件程度のフォルダーでもUIが固まらない
- アクセス権がない場所でクラッシュしない
- ファイルの追加・削除・名前変更が画面へ反映される
- シンボリックリンクの循環で停止しない

### Phase 3：Explorer型UIの完成

操作感をExplorerへ近づける。

実装対象：

- 列の表示・非表示・並べ替え
- 列幅の保存
- 複数選択と範囲選択
- 右クリックメニュー
- インライン名前変更
- ドラッグ＆ドロップ
- フォルダーツリーと一覧の同期
- アドレスバーのパンくず表示と直接編集
- ステータスバー
- プレビューペイン
- 表示設定の永続化

推奨ショートカット：

- `F2`：名前変更
- `Delete`：ゴミ箱
- `Return`：開く
- `Command + L`：パスバーへ移動
- `Command + T`：新規タブ
- `Option + ↑`：親フォルダー
- `Space`：Quick Look

Windows風キーマップでは、可能な範囲でWindowsのキー操作も追加する。

### Phase 4：ファイル操作エンジン

UIとは独立したファイル操作キューを作る。

実装対象：

- コピー
- 移動
- 切り取り／貼り付け
- 名前変更
- 新規フォルダー
- ゴミ箱への移動
- 複数操作のキュー
- 進捗表示
- キャンセル
- エラーからの再試行

競合時の選択肢：

- 上書き
- スキップ
- 両方保持
- 新旧情報を比較
- 以降すべてに適用

安全要件：

- コピー完了前に元ファイルを削除しない
- 同一ボリュームと異なるボリュームを区別する
- 操作途中の失敗を記録する
- アプリ終了時に未完了操作を警告する
- 権限不足、容量不足、切断を明確に表示する
- 大きなファイルでもメインUIを停止させない

### Phase 5：macOS統合

- Quick Look
- Spotlight検索
- 「このアプリで開く」
- Finderで表示
- パスをコピー
- AirDrop／共有シート
- macOSタグ
- iCloud Drive
- 最近使った項目
- エイリアス
- ゴミ箱
- 外付けドライブの取り出し

### Phase 6：検索と絞り込み

検索を2種類に分ける。

- 現在のフォルダー内を即時フィルター
- Spotlightを利用した横断検索

追加候補：

- 拡張子
- 種類
- サイズ
- 更新日時
- タグ
- 正規表現
- 検索条件の保存

### Phase 7：ネットワークと高度な機能

MVP後に追加する。

- SMB／NAS
- ネットワーク切断からの復帰
- SFTP
- 2ペイン表示
- フォルダー比較
- 一括名前変更
- チェックサム
- ターミナルで開く
- 圧縮／展開
- 操作の取り消し
- プラグインまたはカスタムアクション

## 4. テスト計画

最低限、以下を自動テストまたは手動テスト項目にする。

- 空フォルダー
- 1万件以上のフォルダー
- 日本語、絵文字、長いファイル名
- 隠しファイル
- シンボリックリンク
- macOSアプリのパッケージ
- 読み取り専用ファイル
- アクセス権のないフォルダー
- 同名ファイルの競合
- 容量不足
- 外付けドライブの突然の切断
- コピー中のアプリ終了
- SMBの通信切断
- iCloud上で未ダウンロードのファイル
- ドラッグ中の移動元・移動先変更

## 5. 推奨フォルダー構成

```text
ExplorerForMac/
├── ExplorerForMac.xcodeproj
├── App/
├── Core/
│   ├── FileSystem/
│   ├── FileOperations/
│   ├── Navigation/
│   └── Models/
├── Features/
│   ├── FolderTree/
│   ├── FileList/
│   ├── AddressBar/
│   ├── Tabs/
│   ├── Preview/
│   ├── Search/
│   └── Settings/
├── Infrastructure/
│   ├── Persistence/
│   ├── Security/
│   └── MacIntegration/
├── Tests/
│   ├── UnitTests/
│   └── UITests/
├── docs/
│   ├── product-requirements.md
│   ├── explorer-behavior-matrix.md
│   ├── keyboard-shortcuts.md
│   ├── file-operation-rules.md
│   └── architecture.md
└── scripts/
```

## 6. 最初の作業単位

1. `docs/product-requirements.md`を作る
2. Explorerの画面と操作を挙動単位で一覧化する
3. メイン画面のワイヤーフレームを決める
4. Xcodeプロジェクトを作成する
5. 3ペインの空UIを実装する
6. 任意フォルダーの内容を読み取り専用で表示する
7. 戻る、進む、上へ、パス入力を実装する
8. タブと状態保存を追加する
9. 閲覧機能をテストしてからファイル変更機能へ進む

最初の開発目標は、**ファイルを一切変更しないExplorer風ブラウザーを完成させること**とする。その後、独立したファイル操作エンジンを追加する。
